export enum SessionEndpoints {
  LOGIN = '/api/sessions',
  LOGOUT = '/api/sessions/',
}

export enum BuildingEndpoints {
  BUILDINGS = '/api/buildings',
  BUILDING = '/api/buildings/',
}

export enum ManagerEndpoints {
  MANAGERS = '/api/managers',
  MANAGER = '/api/managers/',
}

export enum AdministratorEndpoints {
  ADMINISTRATORS = '/api/administrators',
  ADMINISTRATOR = '/api/administrators/',
}

export enum StaffEndpoints {
  STAFFS = '/api/staffs',
  STAFF = '/api/staffs/',
}
export enum CategoryEndpoints {
  CATEGORIES = '/api/categories',
  CATEGORY = '/api/categories/',
}

export enum CompanyEndpoints {
  COMPANIES = '/api/companies',
  COMPANY = '/api/companies/',
}

export enum InvitationEndpoints {
  INVITATIONS = '/api/invitations',
  INVITATION = '/api/invitations/'
}

export enum ApartmentEndpoints {
  APARTMENTS = '/api/apartments',
  APARTMENT = '/api/apartments/'
}

export enum OwnerEndpoints {
  OWNERS = '/api/owners',
  OWNER = '/api/owners/'
}

export enum TaskEndpoints {
  TASKS = '/api/tasks',
  TASK = '/api/tasks/'
}

export enum ImporterEndpoints {
  IMPORTERS = '/api/importers',
  IMPORTER = '/api/importers/import'
}

export enum ReportEndpoints {
  REPORTS_STAFF = '/api/reports/byStaff',
  REPORTS_BUILDINGS = '/api/reports/byBuilding'
}
